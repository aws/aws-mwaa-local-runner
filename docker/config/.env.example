# =============================================================================
# MWAA Local Runner Environment Configuration
# =============================================================================
# Copy this file to .env.localrunner and fill in your personal values:
#   cp .env.example .env.localrunner
#
# The .env.localrunner file is gitignored - your personal config stays local.
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: Fill these in with your personal values
# -----------------------------------------------------------------------------

# Your AWS profile name (run `aws sso login --profile <profile>` before starting)
PIPELINE_AWS_PROFILE=your-profile-name
AWS_PROFILE=your-profile-name

# Your Snowflake username (usually your email)
SNOWFLAKE_USERNAME=your-email@springhealth.com

# -----------------------------------------------------------------------------
# Airflow Configuration
# -----------------------------------------------------------------------------

# Default admin password for Airflow UI (admin/admin)
DEFAULT_PASSWORD=admin

# Airflow variables (prefix with AIRFLOW_VAR_)
AIRFLOW_VAR_CENSUS_COMPANIES_REVIEW=test-customer

# Python path for virtualenv tasks to find local modules
PYTHONPATH=/usr/local/airflow

# -----------------------------------------------------------------------------
# SSL/Zscaler certificate configuration
# -----------------------------------------------------------------------------
REQUESTS_CA_BUNDLE=/usr/local/airflow/ca_certs/zscaler-custom-ca-bundle.pem
SSL_CERT_FILE=/usr/local/airflow/ca_certs/zscaler-custom-ca-bundle.pem
CURL_CA_BUNDLE=/usr/local/airflow/ca_certs/zscaler-custom-ca-bundle.pem