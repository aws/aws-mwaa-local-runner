--constraint "https://raw.githubusercontent.com/apache/airflow/constraints-2.10.3/constraints-3.11.txt"

# Airflow providers (must match MWAA version)
apache-airflow-providers-snowflake==5.8.0
apache-airflow-providers-amazon==9.0.0
apache-airflow-providers-mysql==5.7.3

# Required for @task.virtualenv
virtualenv

# Snowflake connector (needed for providers)
snowflake-connector-python>=3.10.0

# Required for loading .env files in constants modules
python-dotenv==1.0.1

# NOTE: Additional requirements are auto-installed from dp_airflow_pipelines
# on container startup (see startup_script/startup.sh)
